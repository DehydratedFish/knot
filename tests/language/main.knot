_write :: (fd: s32, buffer: u8&, size: u32) #foreign;

func :: (num: s32): s32 {
    return num + 2;
}

main :: (): int {
    str := "Hello";

    _write(1, str.data, str.size);

    return func(42);
}

